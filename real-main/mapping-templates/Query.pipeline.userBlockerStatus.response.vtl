#if ($ctx.error)
  $util.error($ctx.error.message, $ctx.error.type)
#end

#if ($util.isNull($ctx.result))
  #set ($status = 'NOT_BLOCKING')
#else
  #set ($status = 'BLOCKING')
#end

#set ($user = $ctx.prev.result)
$util.qr($user.put('blockerStatus', $status))
$util.toJson($user)
