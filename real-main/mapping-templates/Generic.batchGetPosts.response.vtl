#if ($ctx.error)
  $util.error($ctx.error.message, $ctx.error.type)
#end

#foreach ($post in $ctx.result.data.${table})
  #if (! $util.isNull($post))
    #set ($index = $ctx.stash.postPkToIndex[$post.partitionKey])
    $util.qr($ctx.stash.posts.items.set($index, $post))
  #end
#end

{}
