#if ($ctx.error)
  $util.error($ctx.error.message, $ctx.error.type)
#end

#set ($userIds = [])
#foreach ($item in $ctx.result.items)
  $util.qr($userIds.add($item.userId))
#end

#set ($ctx.stash.nextToken = $ctx.result.nextToken)
#set ($ctx.stash.comments = $ctx.result.items)
$util.toJson($userIds)
